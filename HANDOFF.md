# Handoff — АРМАДА учёт перевозок

## ⚠ Правило агента (обязательно)
См. `AGENT_RULES.md`.

1. **Брать:** `https://github.com/laser-erp/armada-perevozki` (`git clone` / `git pull`)
2. **Сохранять:** туда же (`git push origin main`) после каждого блока + zip в `/agent/backups/…-latest.zip`
3. План → «ок» → код. Не восстанавливать проект по чату.
4. **База знаний пользователя:** `KNOWLEDGE_BASE.md` — обновлять после каждого блока (см. `AGENT_RULES.md`).

## Роли
- Splash → Водитель / Администратор
- Админ PIN: `2580`
- Водитель не видит: нулевой км, с грузом, до стоянки, сумму заправки, финансы

## Открытая смена / ЕТО
- Если есть незакрытая смена — при входе она продолжается
- **ЕТО действительно только в календарный день прохождения**; на следующий день — осмотр заново (авто оставляем, одометр/топливо/жидкости/свет/масло — заново)
- Ночь с грузом: вчера закрыли смену («осталась загружена») → сегодня новая смена + ЕТО → закрыть перенесённый заказ после выгрузки
- «Новая смена» когда открытой смены нет

## Топливо при закрытии заказа (готово)
- Заправка «Да» → ₽/л + залитые литры (`fuelLiters`)
- Заправка «Нет» → ₽/л с прошлой заправки, иначе **80 ₽/л**; литры заправки не пишем
- Остаток бака (`fuelRemainingLiters`):
  - до заказа: прошлый заказ по авто, иначе ЕТО смены (**не** 80 л)
  - расход = км цепочки (нулевой+груз+до стоянки) × норма / 100
  - «Нет»: `остаток = остаток − расход` (если был остаток)
  - «Да»: `остаток = остаток − расход + залито`
  - после закрытия обновляем `shift.fuelLiters` для следующего заказа дня

## Закрытие смены (готово)
- «Закрыть смену» → одометр на стоянке → `emptyKmAfter` в последний закрытый заказ
- Нельзя закрыть смену при незакрытом заказе, **кроме**: машина осталась загружена, выгрузка завтра
  - Спрашиваем Да/Нет → при Да заказ помечается `staysLoadedOvernight`, `overnightNights += 1`, смена закрывается
  - На следующий день: новая смена + ЕТО → «Закрыть заказ» после выгрузки; админ ставит ₽/ночь хранения
- Начать заказ / создать заказ: только при открытой смене и завершённом ЕТО

## Заявки водителя — выезд / прибытие (готово)
- Назначенный заказ: **1) Выехал** (одометр со стоянки) → статус «В пути» → **2) Прибыл на загрузку** (одометр) → «В работе»
- Нулевой до заказа = одометр на загрузке − база (ЕТО / прошлый заказ)
- Яркий баннер у водителя, пока есть «Назначен» / «В пути»
- Свайп в «Заявки»: «Выехал» / «Прибыл»; кнопки в чате после ЕТО
- Нельзя закрыть смену / взять другой заказ, пока «В пути»

## Заказчики и адреса (готово)
- Справочник заказчиков: имя + адреса загрузок/выгрузок
- Запоминаются при сохранении заявки; в создании/карточке — выбор

## Компании / перевозчик / биржа (готово в вебе)
- Карточка компании: роли `наша фирма` / `заказчик` / `перевозчик`
- Наши фирмы по умолчанию: ООО «Армада», ИП Нечаев А.С.; в заявке — поле «От нашей фирмы»
- Заказчик: адреса, контактные лица (ФИО, должность, телефон)
- Перевозчик: контакты, водители, ТС (номер, марка, т, габариты Д×Ш×В м)
- Создание заявки: контакт; исполнение **наш / перевозчик / биржа**
- Биржа: админ включает `Биржа` у нашего водителя; водитель видит и берёт заказ
- **Админ → фильтр «Биржа»**: все заказы на бирже с полными данными (км, ставки, ГСМ, ЗП, водители с биржей); назначить / снять с биржи
- Водителю **название заказчика не показываем**; контакт — только после «Выехал» / взятия в работу; на бирже до взятия — только маршрут и время
- В заявке: **Подача ТС** (дата/время); **ориентир освобождения** = подача + мин. часы работы (4 ч; или больше, если задан ориентир/факт часов)
- Финансы водителю не показываем
- iOS: модели + биржа в «Заявки» + флаг у водителя + фильтр «Биржа» у админа
- Позже: вход водителя по телефону, доступ водителей перевозчика

## Админ-таблица
Ставки, ГСМ, подушка 10%, ЗП, аренда, прибыль, справочники.
Ставки: ввод в одно поле → остальные авто:
- с НДС = без НДС + 22% (НДС 22%)
- нал = без НДС − 8%
- **ЗП / подушка — от ставки наличные без ночного хранения**; прибыль — от полной суммы клиенту
- форма оплаты — для клиента/документов, не для ЗП

## Маршрут с доп. точками (готово)
- Точка = адрес + тип (`загрузка` / `выгрузка`)
- При «+ Точка» выбирается тип; тип можно сменить у любой точки
- Порядок меняется ↑ ↓; удаление промежуточных (минимум 2)
- Отображение: `Загрузка: А → Выгрузка: Б → …`
- Старые заявки (строки): первая → загрузка, последняя → выгрузка
- Водитель видит подписанный маршрут (без редактирования)

## Тариф клиенту — пакет (готово)
- Мин. заказ: **4 ч** работы + **1 ч** подачи (₽/час)
- Нулевой **до** заказа: если `до×₽/км ≤ 1×₽/час` — покрыт 1 ч подачи; иначе **2 ч** подачи (₽/км нулевого до не плюсуем)
- В пакет входит **100 км** = груз + нулевой **после** (до стоянки); свыше — **80 ₽/км** (настраивается)
- Часы/км сверх пакета — по факту; если не превысили — сумма пакета без изменений
- Ночное хранение сверху (не в ЗП)
- Водителю финансы не показываем

## Ночное хранение груза (готово)
- При «машина осталась загружена» → `overnightNights += 1`, заказ переносится
- В карточке заявки админ задаёт **₽/ночь** и число ночей
- Сумма хранения входит в тариф клиенту (нал / без НДС / с НДС)
- **ЗП и подушка** считаются от нал **без** хранения (ответственность компании)
- Прибыль — от полной суммы клиенту (хранение остаётся компании)

## Себестоимость / наценка (готово)
- Фикс = ГСМ + аренда + доплата ЗП
- Безубыток = фикс / (1 − %ЗП − подушка 10%)
- Рекомендация = фикс / (1 − %ЗП − подушка − наценка%)
- Наценка по умолчанию **15%**, меняется в Справочники → Финансы (0–80)
- В карточке заявки: себестоимость (фикс), полная, безубыток, рекомендация

## ЗП водителю / личный кабинет (готово)
- Кнопка «Кабинет»: итоговая сумма «Заработано»
- Расшифровка: дата, № заказа (база + день), авто, маршрут, сумма
- Блок «Ожидают расчёта» для закрытых без ставки
- Водитель не видит ставки, подушку, прибыль и т.п.

## Ещё не сделано
- Выбор водителя при входе / вход по телефону; водители перевозчика в приложении
- Вернуть заказ на биржу; безопасный merge при синхронизации
- Экспорт CSV / Google Sheets (на паузе)

## Пути
- iOS / web: `/agent/DriverReport/`
- **База знаний (инструкции):** `KNOWLEDGE_BASE.md`
- Бэкапы: `/agent/backups/DriverReport-backup-latest.zip`
- **Главное приложение (общая база):** http://aptown1.fvds.ru/
- GitHub Pages редиректит туда: https://laser-erp.github.io/armada-web/
- Данные: PocketBase на VPS, коллекция `app_state` (key=`main`); веб пишет при каждом `persist()`
- Веб-файлы на сервере: `/var/www/armada` (Caddy :80 → static + `/api`/`/_` → PocketBase)
- **Сайты и пароли:** `/agent/backups/ARMADA_SITES_AND_PASSWORDS.txt`
- HTTPS пока нет (лимит LE на fvds.ru); позже — свой домен

## GitHub
- Repo: https://github.com/laser-erp/armada-perevozki
- Account: laser-erp
- Агент: https://cursor.com/agents/bc-019fb4d1-f788-7cd1-a617-8a118a0f4317

## Следующий блок
- HTTPS: свой домен (`armada.sx` / `app.armada.sx`) → A на `176.12.67.35` → Caddy; либо повтор LE на fvds.ru
- Вход водителя по телефону; водители перевозчика
- Главный вход: http://aptown1.fvds.ru/
- Пароли/ссылки: `/agent/backups/ARMADA_SITES_AND_PASSWORDS.txt`
