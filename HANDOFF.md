# Handoff — АРМАДА учёт перевозок

## ⚠ Правило агента (обязательно)
См. `AGENT_RULES.md`.

1. **Брать:** `https://github.com/laser-erp/armada-perevozki` (`git clone` / `git pull`)
2. **Сохранять:** туда же (`git push`) после каждого блока + zip в `/agent/backups/DriverReport-backup-latest.zip`
3. План → «ок» → код. Не восстанавливать проект по чату.
4. **База знаний пользователя:** `KNOWLEDGE_BASE.md` — обновлять после каждого блока.

**Сессия сохранена:** 2026-08-04 · live build `2026-08-04-firm-claim` · агент https://cursor.com/agents/bc-019fb4d1-f788-7cd1-a617-8a118a0f4317

## Где код сейчас
- Локально: `/agent/DriverReport/` (веб: `web-preview/index.html`)
- Live: http://aptown1.fvds.ru/ → `/var/www/armada/index.html` на `176.12.67.35`
- GitHub: https://github.com/laser-erp/armada-perevozki
- Ветка этой сессии: **`cursor/firm-exchange-claim-4317`** (PR #43)
- Предыдущая ветка с парком/фирмами: `cursor/admin-own-orders-4317` (PR #42, ещё может быть открыт)
- Бэкап: `/agent/backups/DriverReport-backup-latest.zip`
- Секреты: `/agent/backups/ARMADA_SITES_AND_PASSWORDS.txt` (не в git)

## Роли / PIN
| Кто | Вход |
|-----|------|
| Супер | Наволоцкий Е.Н. · PIN **`45680`** |
| Админ | Нечаев А.С. · PIN **`1994`** |
| Водитель | кнопка «Водитель» (пока всегда как Наволоцкий Е.Н.) |

Сессия админа в `localStorage` (`armada_admin_session_v1`) — после refresh не выкидывает сразу.

## Фирмы / парк (готово)
- У каждого админа своё пространство + «наша фирма» (`spaces`, `ownCompanyId`)
- Водители и авто привязаны к `companyId` / `spaceId` — **парк у каждой фирмы свой**
- Одно ФИО может быть в нескольких фирмах (разные записи); удаление только в своей фирме
- В **«+ Заказ»** авто/водитель — только парк выбранной «От нашей фирмы»
- У авто: госномер, л/100, **т / Д×Ш×В** (тоннаж нужен для биржи)

## Биржа между фирмами (готово, build firm-claim)
1. Фирма А: лишний заказ → «На биржу» (нужна грузоподъёмность в требованиях к ТС)
2. Фирма Б: фильтр **Биржа** → **Забрать**
3. Электронная **договор‑заявка** (А = заказчик перевозки, Б = перевозчик)
4. Б назначает **своего** водителя и авто (≥ т и габаритам заявки)
5. В заказе: `transportApp`, статус **Партнёр**, видят обе стороны

Свой заказ на бирже: назначить себе / снять. Водительская биржа (флаг «Б» у водителя) по-прежнему отдельно.

## Sync / отмена
- `dataEpoch` + preflight push: старая вкладка не затирает новую
- Отмена удаляет заказ из списка
- PocketBase `app_state` key=`main`

## Ещё не сделано
- Вход водителя по телефону / выбор водителя (сейчас захардкожен Наволоцкий)
- HTTPS / свой домен `armada.sx`
- Более безопасный merge по id заказа при sync
- Экспорт CSV / Google Sheets (пауза)

## Пути
- Главное приложение: http://aptown1.fvds.ru/
- Веб на VPS: `/var/www/armada`
- PocketBase: `/opt/pocketbase`, прокси `/api` `/_`
- Инструкции: `KNOWLEDGE_BASE.md`
- Пароли: `/agent/backups/ARMADA_SITES_AND_PASSWORDS.txt`

## Следующий блок (завтра)
- Проверить сценарий Армада → биржа → Нечаев «Забрать» на телефоне
- У ИП Нечаева завести авто с т/габаритами, если ещё нет
- При необходимости смержить PR #43 / #42 в `main`
