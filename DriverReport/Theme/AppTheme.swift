import SwiftUI

enum AppTheme {
    /// Armada brand
    static let canvasTop = Color.black
    static let canvasBottom = Color(red: 0.12, green: 0.12, blue: 0.12)
    static let accent = Color(red: 0.90, green: 0.10, blue: 0.12)
    static let botBubble = Color(red: 0.18, green: 0.18, blue: 0.18)
    static let driverBubble = Color(red: 0.90, green: 0.10, blue: 0.12)
    static let textPrimary = Color.white
    static let textMuted = Color.white.opacity(0.72)
    static let field = Color.white.opacity(0.10)
    static let charcoal = Color(red: 0.22, green: 0.22, blue: 0.22)
}
