---
description: Staging перед продом
alwaysApply: true
---

# Staging перед продом

Прод `deploy-fvds.sh` и merge в `main` — **только после «ок»** Евгения после проверки на https://staging.app.armada.sx/ (`deploy-staging-fvds.sh`). Те же API/данные — не ломать данные «для теста». См. `scripts/STAGING.md`.
