# Локальный агент: продолжить прогон «Жизнь заявки» (как облачный агент 24.09.2026)

**Для кого:** Cursor на ПК Евгения. **Евгений не программист** — отвечайте коротко по-русски, без жаргона.

**Скопируйте в новый чат локального агента** блок «Задание одной строкой» в конце файла + приложите этот файл (`@ORDER_LIFECYCLE_QA_LOCAL_AGENT.md`).

---

## С чего продолжить (главное)

| Что | Значение |
|-----|----------|
| Окружение | **Только staging** https://staging.app.armada.sx (оранжевая полоса) |
| Заявка прогона | **№9** (space **ООО «Армада»**) |
| Последний **ЗАМОК** | **Шаг 1** — создание + «Входящие» (24.09, проверил Евгений) |
| **Сейчас** | **Шаг 2** — карточка, цены, грузоотправитель, печать → ждём **ДА** от Евгения |
| **Дальше** | Шаг 3 — назначить водителя + ТС → колонка «Назначен» |

Журнал для человека: **`ORDER_LIFECYCLE_QA_RUN.md`**.  
Техника + файлы: **`ORDER_LIFECYCLE_QA_AGENT.md`**.  
ЗАМКИ и «не трогать код»: **`ORDER_LIFECYCLE_QA_ISOLATION.md`**.  
Полный чеклист: **`ORDER_LIFECYCLE_QA_PLAN.md`**.

---

## Правила работы (все инструкции на сегодня)

### Процесс одной задачи

1. **План → «ок» → код.** Одна задача за раз, без «заодно».
2. **Grep → символ → Read кусок** — не читать целые `store.js` / `admin.js` / `app.js`.
3. Карта входов: **`AGENT_NAVIGATION.md`** — сначала таблица; после нового файла/символа — **одна строка** в карту.
4. Ядро правил: **`AGENTS.md`**, **`.cursor/rules/00-core.mdc`**.

### Git — одна ветка (локальный = облако)

- Рабочая ветка: **`cursor/dev-f6d2`** только.
- **Первое действие агента:** git **из корня открытой папки Cursor** (где лежат `AGENTS.md` и `.git`), **не** из `C:\Users\Жека`:
  `git fetch origin && git checkout cursor/dev-f6d2 && git pull origin cursor/dev-f6d2`
  Если `fatal: not a git repository` — сказать Евгению открыть в Cursor папку **`armada-perevozki`**, не домашний каталог.
- Push: **только** если Евгений сказал «пуш» / «коммить» (облако по умолчанию пушит в dev-ветку).
- **Не** создавать `cursor/новая-задача-f6d2` на каждую правку.
- Доки: `scripts/GIT_ONE_BRANCH.md`, `scripts/SYNC_FROM_GITHUB.md`, `scripts/CLOUD_AGENT_GIT.md`.
- PR в main: черновик **#149** (`cursor/dev-f6d2` → `main`).

### Staging и prod

- **Любые новые правки UI/скриптов** — сначала **staging**: `./scripts/deploy-staging-fvds.sh` (см. `scripts/STAGING.md`).
- Пароль VPS: секрет **`root`** или `FVDS_SSH_PASSWORD` — **не спрашивать** у Евгения, деплоить staging после правок если он просил проверить на staging.
- **Prod** (`deploy-fvds.sh`, https://app.armada.sx) — **только** после проверки на staging и явных слов: «деплоим на prod», «ок на prod». Prod **без order-fix** до этого «ок».

### Данные и QA

- База **общая**. Тесты, удаления, массовые чистки — **только space ООО «Армада»** (`findArmadaLogistCompany()`).
- **Не трогать** заказы **ИП Нечаев**, **МБН** и другие кабинеты без отдельной просьбы.
- На прогон — **одна заявка** (сейчас **№9**) на цепочку шагов 1–8.

### ЗАМКИ (изоляция шагов)

- После **ДА** Евгения на шаг **N** → в **`ORDER_LIFECYCLE_QA_RUN.md`**: 🔒 в таблице + блок **ЗАМКИ** (дата, APP_BUILD, № заявки, что работает).
- **Не править** файлы под ЗАМОКом шагов ≤ N без фразы **«да, чини шаг N»**.
- Один коммит ≈ один **QA-STEP** (`QA-STEP: 1`, `1b`, `2`, …) — не смешивать шаг 1 и шаг 3.
- Preflight (read-only):  
  `BASE_URL=https://staging.app.armada.sx ./scripts/preflight-order-lifecycle-qa.sh`

### Стоп и коммиты

- Задача сделана → короткий ответ → **конец**. Без фонового deploy/push без слов Евгения.
- Коммиты на **русском** (`.cursor/rules/commit-messages-ru.mdc`).

### Запреты (AGENTS.md)

- Подагенты, explore всего репо, best-of-N.
- Не открывать без запроса: `docs/HANDOFF.archive.md`, `docs/TASK_*.done.md`.
- Не трогать: Saby, JaCarta, Dr.Web, AnyDesk, SSH-туннель, копию рабочего стола в планировщике.

---

## Что уже сделано в коде (24.09, ветка `cursor/dev-f6d2`)

### Шаг 1a — форма `order.html` → сервер

- `order.html` — только `order-public.js` + `store.js`, без полного `app.js`.
- Немедленное сохранение: `persistCustomerPortalOrderImmediate` в `store.js`.
- SW: `sw.js` — `swResponseOrError` (B3).
- Staging build (история): `2026-09-24-order-public-sync-v6`.

### Шаг 1b — канбан «Входящие»

- `app.js`: `healPhantomPortalTrip`, `healPortalInboxDriverStub`; колонка через `isLogistInboxOrder` / `adminKanbanColumnKey`.
- **№9** после Ctrl+Shift+R — во **«Входящих»**, не в «В работе».

### Шаг 2 (код выкатан, **ЗАМОК ещё нет** — ждём проверку Евгения)

**`admin.js` — карточка заказа:**

- Секции: **Заказчик**, **Груз и ТС**, **Цена**, **Маршрут**.
- Парк: подписи «Назначьте водителя» / «Выберите ТС».
- Требования к ТС: `adminHydrateOrderReqsForDisplay`, `mapVtypeToBodyType`; список «Груз» скрыт (`#d-cargo-kind`).
- ИНН — только **юр. лицо** (10 цифр); для **физ. лица** поле скрыто.
- Перевозчик по умолчанию — **своя фирма** (`adminDefaultCarrierId`).
- Цены: тариф → перевозчик; заказчик **×1.35** (`ADMIN_LOGIST_CLIENT_MARKUP`); кнопка **«Пересчитать из тарифа»**; маржа видна в карточке.
- **`#d-route-km`**: placeholder «авто» — **автозаполнение км в карточке пока нет** (в форме создания км считается через `estimateRouteGeometry` / `refreshCreateRouteKm` — отдельная задача, если попросят).

**`order-documents.js` — печать:**

- На бланке для **заказчика** — только **сумма заказчика** (`priceForClient`, `orderDocCustomerAmount`).
- **Сумма перевозчика** — только при печати договор-заявки с аудиторией **«Перевозчик»** (`orderDocCarrierAmount`, кнопки **Заказчик** / **Перевозчик** в `admin.js`).
- Портал заказчика — документы только `audience: customer`.

**Текущий `APP_BUILD` в репо:** `2026-09-24-doc-carrier-transport-app-v10` (`store.js`). На staging после деплоя смотреть F12 / консоль «АРМАДА build …».

---

## Что проверяет Евгений на шаге 2 (staging, №9)

Путь: `/a` → **Заказы** → **Входящие** → **№9** → **Карточка**. Обновление: **Ctrl+Shift+R**.

1. Маршрут, груз, заказчик на месте.
2. **Грузоотправитель** — при необходимости отдельно от заказчика → **Сохранить**.
3. **Цены** заказчику и перевозчику, видна **маржа**; «Пересчитать из тарифа».
4. **Печать** договор-заявки: кнопка **Заказчик** — без суммы перевозчика; **Перевозчик** — с суммой перевозчика.
5. Обновить страницу — данные **сохранились**.

Когда Евгений пишет **«карточка ок…» + ДА**:

1. Записать **ЗАМОК шаг 2** в `ORDER_LIFECYCLE_QA_RUN.md` (таблица + блок ЗАМКИ, APP_BUILD staging).
2. Перейти к **шагу 3** по `ORDER_LIFECYCLE_QA_PLAN.md` § сценарий 1.

Если FAIL — правки только под шаг 2; **не** трогать файлы шага 1 без «да, чини шаг 1».

---

## Шаги 3–8 (кратко, после ЗАМОКа 2)

| Шаг | Смысл | Основные файлы |
|-----|--------|----------------|
| 3 | Назначение водитель + ТС → «Назначен» | `admin.js`, `store.js` (`persistOrderAssignmentImmediate`) |
| 4 | ЭТrН T1–T4 | **N/A** для тарифа «Старт» / Армада без модуля |
| 5 | Рейс водителя `/v` | `driver.js`, смены `store.js` |
| 6 | Закрытие рейса | `driver.js`, `looksClosedOrder`, канбан «Закрыт» |
| 7 | Документы, акт | `order-documents.js`, `admin.js` |
| 8 | Закрытие смены | `driver.js` |

Детальный чеклист — **`ORDER_LIFECYCLE_QA_PLAN.md`**, сценарий 1.

---

## Известные баги / заметки (B1–B5)

См. таблицы в **`ORDER_LIFECYCLE_QA_AGENT.md`** (шаг 1). Не регрессировать после ЗАМОКа 1.

---

## Деплой staging (после правок)

```bash
cd /путь/к/armada-perevozki
git checkout cursor/dev-f6d2
chmod +x scripts/*.sh
./scripts/deploy-staging-fvds.sh cursor/dev-f6d2
# или main — см. STAGING.md; для QA обычно dev-ветка с последними правками
```

Отчёт Евгению: URL staging, **APP_BUILD**, что именно проверить, № заявки **9**.

---

## Опционально (не делать без просьбы)

- Закрыть старый PR **#148** (order-public-sync) — дублирует dev-ветку.
- Merge **#149** в main + prod — только «ок на prod».
- Авто **км** в карточке заказа (как в форме создания).

---

## Задание одной строкой (вставка в чат локального агента)

См. **`ORDER_LIFECYCLE_QA_COPY_LINE.txt`** в корне репо — одна строка для копирования.
