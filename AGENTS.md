# AGENTS.md — АРМАДА (фаза 0, читать первым)

Бюджет ограничен (~$10). Цель: **не читать 100k+ tok** на одну задачу.

## Источник кода
- Репо: `https://github.com/laser-erp/armada-perevozki`
- **Код веб-приложения сейчас:** ветка `cursor/admin-cal-filters-4317` (tip, PR #101).  
  `main` = доки + устаревший HTML — **не** брать UI оттуда.
- Live: http://aptown1.fvds.ru/ → `/var/www/armada/`
- Workspace = **только это репо**. Не открывать Армаду внутри чужой папки/монорепо.

## Процесс
1. План → ждать «ок» / «ок, делай» → код. Без «ок» — ноль правок.
2. **Одна задача.** Без «заодно», без рефакторинга, без merge/распила без явного ок.
3. Мелкая правка / та же тема → **тот же чат**. Новый чат — только смена темы (и это платная опция).
4. Ответы коротко. В конце среза — оценка tok (вход/выход).

## Запреты (фаза 0)
- Подагенты, explore «по всему репо», best-of-N, параллельные агенты
- Читать **весь** `web-preview/index.html` (~550 KB / ~130k+ tok)
- Тянуть iOS в веб-таск без запроса
- Длинные саммари HANDOFF / KNOWLEDGE_BASE «на всякий случай»

## Как читать код
- Только нужный фрагмент: **файл + функция/символ + строки** (Grep → Read offset/limit).
- Веб: `web-preview/index.html` — точечно (`adminOrders*`, `renderAdmin`, …), не целиком.
- Доки: этот файл → при нужде раздел `AGENT_RULES.md`. KB/HANDOFF — **по запросу пользователя** или если без них нельзя закрыть баг.

## Модель
- По умолчанию: быстрая / non-thinking (Composer и аналоги).
- Дорогой thinking — только жёсткий баг по просьбе пользователя.

## TASK (если новая тема)
Шаблон: `TASK_TEMPLATE.md`. Файл загрузки: `START_NEW_CHAT.txt` (не дефолт на каждую правку).

## Дальше (не делать в фазе 0)
- Фаза 1: merge tip → один `main`/`develop`
- Фаза 2: распил `index.html`
- Фаза 3: сжать доки ещё сильнее  

Только после отдельного «ок».
