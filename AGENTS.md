# AGENTS.md — АРМАДА (фаза 0+1)

Бюджет ограничен (~$10). Цель: **не читать 100k+ tok** на одну задачу.

## Источник кода (фаза 1)
- Репо: `https://github.com/laser-erp/armada-perevozki`
- **Код веб = `main`** (влит tip `admin-cal-filters` / PR #101). Live деплой только с `main`.
- Старые `cursor/*-4317` — не источник правды.
- Live: http://aptown1.fvds.ru/ → `/var/www/armada/`
- Workspace = **только это репо**. Не открывать Армаду внутри чужой папки/монорепо.

```bash
cd /agent/DriverReport && git fetch origin && git checkout main && git pull origin main
```

## Процесс
1. План → ждать «ок» / «ок, делай» → код. Без «ок» — ноль правок.
2. **Одна задача.** Без «заодно», без распила без явного ок.
3. Мелкая правка / та же тема → **тот же чат**. Новый чат — только смена темы.
4. Ответы коротко. В конце среза — оценка tok (вход/выход).

## Запреты
- Подагенты, explore «по всему репо», best-of-N, параллельные агенты
- Читать **весь** `web-preview/index.html` (JS; стили в `styles.css`)
- Тянуть iOS в веб-таск без запроса
- Длинные саммари HANDOFF / KNOWLEDGE_BASE «на всякий случай»

## Как читать код
- Только фрагмент: **файл + функция/символ + строки** (Grep → Read offset/limit).
- Доки: этот файл → при нужде `AGENT_RULES.md`. KB/HANDOFF — по запросу.

## Модель
- По умолчанию: быстрая / non-thinking.
- Дорогой thinking — только жёсткий баг по просьбе.

## TASK
Шаблон: `TASK_TEMPLATE.md`. Новый чат: `START_NEW_CHAT.txt` только при смене темы.

## Дальше (не фаза 1)
- Фаза 2: распил `index.html` — только после «ок, делай»
- Фаза 3: ещё сжать доки
