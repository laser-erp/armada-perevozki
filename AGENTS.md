# AGENTS.md — АРМАДА

**Читать первым.** Правила от Евгения (2026-08-27). Жёсткие. Без исключений.

Бюджет ~$10. Не читать 100k+ tok / целые модули.

## ⛔ Сделал задачу → ОСТАНОВИСЬ

1. **Не продолжать** работу «сам по себе» между сообщениями пользователя (в т.ч. goal / strategic plan).
2. **Не мониторить** GitHub, прод, smoke, sync, O-02, трекер, bundle — **без явного запроса**.
3. **Не крутить** `run-strategic-verification`, `sync-o02-github-status`, deploy, push в фоне «на всякий случай».
4. **Не трогать** `docs/STRATEGIC_PLAN_2026.md`, `.cursor/stores/self/strategic-plan-tracker.json`, O-02 — пока пользователь **не попросил**.
5. Задача выполнена → **короткий ответ** → **конец**. Без «следующих шагов», без «я продолжу проверять».

**Задача** = только то, что написано в сообщении («почини кнопку», «пуш»).  
**S0–S7 / стратегический план** — не автозадача. Не возобновлять из system notification без слова пользователя.

**Можно** трогать план / deploy / push / smoke — только если пользователь явно написал: «продолжай план», «проверь прод», «задеплой», «пуш» и т.п.

❌ Закоммитил → smoke → sync → tracker → bundle → ждать.  
✅ Закоммитил → ответил → **стоп**.

## Код и прод

- Репо `laser-erp/armada-perevozki` · live **https://app.armada.sx/** (редирект с aptown1.fvds.ru)
- Рабочая ветка: `cursor/perf-speed-4317` (или та, что в задаче)
- **Деплой на прод:** `./scripts/deploy-fvds.sh` — **только по просьбе** пользователя
- **GitHub push** — **только по «пуш»**; репозиторий держать **Private**. Прод не зависит от GitHub
- Модули: `styles.css` · `store.js` · `driver.js` · `admin.js` · `app.js` · `index.html`
- Grep → символ → Read **кусок**. В TASK: файл + функция + строки

## ⛔ Не ломать · Проверять · Не врать про «работает»

**От пользователя (Евгений), 2026-08-30. Жёсткое.**

1. **Ничего не ломать** из уже сделанного — минимальный diff, без «заодно»; не трогать недавние фиксы (вход `/a`, админы, sync, armada-api) без запроса.
2. **Изменил — проверь:** скрипт/API/браузер по смыслу задачи; не писать «готово», пока проверка не прошла.
3. **Перед «всё работает» — дважды убедись:** первая проверка + вторая (другим способом или повтор); в ответе указать **что именно** проверено, не общими словами.
4. Проверка упала → честно «ещё не работает», без деплоя «на авось».

Подробно: `.cursor/rules/do-not-break-verify.mdc` · админы: `./scripts/verify-admin-list-prod.sh`

## Процесс

1. **Одна** задача. Без «заодно» и лишних улучшений.
2. Та же тема → **этот чат**. Новый чат — смена темы (+ `START_NEW_CHAT.txt` / `TASK_TEMPLATE.md`).
3. Короткий ответ пользователю (он не программист). В конце среза — оценка tok.

## Запреты

- Подагенты · explore всего репо · best-of-N · iOS без запроса
- Всегда тащить KB/HANDOFF · обновлять KB на микрофикс
- Делать то, о чём не просили (инструкции, рефакторинг, «улучшения» UI без запроса)
- Писать в ответах «Edit» для GitHub Deploy key — **только Delete + Add** с Allow write
- Открывать без запроса: `docs/HANDOFF.archive.md`, `docs/TASK_*.done.md`, трекер плана

## Модель

Быстрая по умолчанию. Thinking — только по просьбе на жёсткий баг.

## Доки

- **Этот файл** — единственный always-on для агента
- Дубликат правил: `.cursor/stores/self/00-READ-FIRST-STOP-WHEN-DONE.md`
- `KNOWLEDGE_BASE.md` — для людей; только по запросу
- Письма на бланке: `docs/operator-letters/`, `.cursor/stores/self/armada-letterhead.md`
- Шаблон задачи: `TASK_TEMPLATE.md`
- **Чат vs Форма:** `.cursor/rules/customer-chat-mobile.mdc` — чат без формы в UI; форма заполняется из ответов или вручную в режиме «Форма»
- **Скорость на телефонах:** `.cursor/rules/mobile-performance.mdc` — приложение должно летать на всех устройствах; загрузка, рендер, сеть
